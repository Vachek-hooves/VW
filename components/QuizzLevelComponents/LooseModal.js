import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const LooseModal = () => {
  return (
    <View>
      <Text>LooseModal</Text>
    </View>
  );
};

export default LooseModal;

const styles = StyleSheet.create({});
